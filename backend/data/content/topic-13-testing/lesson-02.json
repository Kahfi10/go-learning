{
  "id": "02",
  "title_id": "Table-Driven Tests",
  "title_en": "Table-Driven Tests",
  "estimatedMinutes": 10,
  "content_id": "## Mengapa Table-Driven Tests?\n\nDaripada menulis fungsi test terpisah untuk setiap kasus, **table-driven tests** mengumpulkan semua kasus dalam sebuah slice dan mengulanginya. Ini adalah pola yang **sangat direkomendasikan** di komunitas Go.\n\n## Pola Dasar\n\n```go\npackage calculator\n\nimport \"testing\"\n\nfunc TestAdd(t *testing.T) {\n\ttests := []struct {\n\t\tname string\n\t\ta, b int\n\t\twant int\n\t}{\n\t\t{\"positive numbers\", 2, 3, 5},\n\t\t{\"negative numbers\", -1, -2, -3},\n\t\t{\"mixed\", -5, 10, 5},\n\t\t{\"zeros\", 0, 0, 0},\n\t}\n\n\tfor _, tt := range tests {\n\t\tt.Run(tt.name, func(t *testing.T) {\n\t\t\tgot := Add(tt.a, tt.b)\n\t\t\tif got != tt.want {\n\t\t\t\tt.Errorf(\"Add(%d, %d) = %d; want %d\", tt.a, tt.b, got, tt.want)\n\t\t\t}\n\t\t})\n\t}\n}\n```\n\n## t.Run untuk Sub-Test\n\n`t.Run(name, func)` membuat sub-test yang:\n- Bisa dijalankan secara individual: `go test -run TestAdd/positive_numbers`\n- Ditampilkan secara terpisah di output verbose\n- Memiliki `*testing.T` tersendiri\n\n## Contoh Lengkap: Testing dengan Error\n\n```go\nfunc TestDivide(t *testing.T) {\n\ttests := []struct {\n\t\tname    string\n\t\ta, b    float64\n\t\twant    float64\n\t\twantErr bool\n\t}{\n\t\t{\"normal division\", 10, 2, 5, false},\n\t\t{\"divide by zero\", 5, 0, 0, true},\n\t\t{\"negative result\", -6, 3, -2, false},\n\t\t{\"fractional result\", 7, 2, 3.5, false},\n\t}\n\n\tfor _, tt := range tests {\n\t\tt.Run(tt.name, func(t *testing.T) {\n\t\t\tgot, err := Divide(tt.a, tt.b)\n\n\t\t\tif (err != nil) != tt.wantErr {\n\t\t\t\tt.Errorf(\"Divide() error = %v, wantErr %v\", err, tt.wantErr)\n\t\t\t\treturn\n\t\t\t}\n\n\t\t\tif !tt.wantErr && got != tt.want {\n\t\t\t\tt.Errorf(\"Divide() = %v, want %v\", got, tt.want)\n\t\t\t}\n\t\t})\n\t}\n}\n```\n\n## Tips\n\n- Nama field struct biasanya `name`, `input`, `want`, `wantErr`\n- Gunakan `tt` sebagai nama variabel loop (convention)\n- Tambahkan kasus edge cases: zero, negative, empty, overflow",
  "content_en": "## Why Table-Driven Tests?\n\nInstead of writing separate test functions for each case, **table-driven tests** collect all cases in a slice and iterate over them. This is a **strongly recommended** pattern in the Go community.\n\n## Basic Pattern\n\n```go\npackage calculator\n\nimport \"testing\"\n\nfunc TestAdd(t *testing.T) {\n\ttests := []struct {\n\t\tname string\n\t\ta, b int\n\t\twant int\n\t}{\n\t\t{\"positive numbers\", 2, 3, 5},\n\t\t{\"negative numbers\", -1, -2, -3},\n\t\t{\"mixed\", -5, 10, 5},\n\t\t{\"zeros\", 0, 0, 0},\n\t}\n\n\tfor _, tt := range tests {\n\t\tt.Run(tt.name, func(t *testing.T) {\n\t\t\tgot := Add(tt.a, tt.b)\n\t\t\tif got != tt.want {\n\t\t\t\tt.Errorf(\"Add(%d, %d) = %d; want %d\", tt.a, tt.b, got, tt.want)\n\t\t\t}\n\t\t})\n\t}\n}\n```\n\n## t.Run for Sub-Tests\n\n`t.Run(name, func)` creates a sub-test that:\n- Can be run individually: `go test -run TestAdd/positive_numbers`\n- Shows up separately in verbose output\n- Has its own `*testing.T`\n\n## Complete Example: Testing with Errors\n\n```go\nfunc TestDivide(t *testing.T) {\n\ttests := []struct {\n\t\tname    string\n\t\ta, b    float64\n\t\twant    float64\n\t\twantErr bool\n\t}{\n\t\t{\"normal division\", 10, 2, 5, false},\n\t\t{\"divide by zero\", 5, 0, 0, true},\n\t\t{\"negative result\", -6, 3, -2, false},\n\t\t{\"fractional result\", 7, 2, 3.5, false},\n\t}\n\n\tfor _, tt := range tests {\n\t\tt.Run(tt.name, func(t *testing.T) {\n\t\t\tgot, err := Divide(tt.a, tt.b)\n\n\t\t\tif (err != nil) != tt.wantErr {\n\t\t\t\tt.Errorf(\"Divide() error = %v, wantErr %v\", err, tt.wantErr)\n\t\t\t\treturn\n\t\t\t}\n\n\t\t\tif !tt.wantErr && got != tt.want {\n\t\t\t\tt.Errorf(\"Divide() = %v, want %v\", got, tt.want)\n\t\t\t}\n\t\t})\n\t}\n}\n```\n\n## Tips\n\n- Struct field names are typically `name`, `input`, `want`, `wantErr`\n- Use `tt` as the loop variable name (convention)\n- Include edge cases: zero, negative, empty, overflow",
  "starterCode": "package main\n\nimport (\n\t\"strings\"\n\t\"testing\"\n)\n\nfunc titleCase(s string) string {\n\tif s == \"\" {\n\t\treturn \"\"\n\t}\n\twords := strings.Fields(s)\n\tfor i, w := range words {\n\t\tif len(w) > 0 {\n\t\t\twords[i] = strings.ToUpper(w[:1]) + strings.ToLower(w[1:])\n\t\t}\n\t}\n\treturn strings.Join(words, \" \")\n}\n\nfunc TestTitleCase(t *testing.T) {\n\ttests := []struct {\n\t\tname  string\n\t\tinput string\n\t\twant  string\n\t}{\n\t\t// TODO: add at least 5 test cases:\n\t\t// 1. normal sentence\n\t\t// 2. already title case\n\t\t// 3. all uppercase\n\t\t// 4. empty string\n\t\t// 5. single word\n\t\t{\"basic\", \"hello world\", \"Hello World\"},\n\t}\n\n\tfor _, tt := range tests {\n\t\tt.Run(tt.name, func(t *testing.T) {\n\t\t\tgot := titleCase(tt.input)\n\t\t\tif got != tt.want {\n\t\t\t\tt.Errorf(\"titleCase(%q) = %q; want %q\", tt.input, got, tt.want)\n\t\t\t}\n\t\t})\n\t}\n}\n\nfunc main() {\n\tt := &testing.T{}\n\tTestTitleCase(t)\n}",
  "quiz": [
    {
      "question_id": "Apa keuntungan utama dari table-driven tests?",
      "question_en": "What is the main advantage of table-driven tests?",
      "options_id": ["Mudah menambah test cases baru tanpa kode duplikat", "Test berjalan lebih cepat", "Tidak membutuhkan package testing", "Otomatis menggenerate test"],
      "options_en": ["Easy to add new test cases without code duplication", "Tests run faster", "No testing package needed", "Automatically generates tests"],
      "correct": 0
    },
    {
      "question_id": "Cara menjalankan sub-test tertentu bernama 'divide by zero' dalam TestDivide?",
      "question_en": "How do you run a specific sub-test named 'divide by zero' inside TestDivide?",
      "options_id": ["go test -run TestDivide/divide_by_zero", "go test TestDivide.divide_by_zero", "go test -sub 'divide by zero'", "go test -filter TestDivide"],
      "options_en": ["go test -run TestDivide/divide_by_zero", "go test TestDivide.divide_by_zero", "go test -sub 'divide by zero'", "go test -filter TestDivide"],
      "correct": 0
    },
    {
      "question_id": "Pola idiomatis untuk memeriksa error dalam test adalah?",
      "question_en": "What is the idiomatic pattern for checking errors in tests?",
      "options_id": ["if (err != nil) != tt.wantErr { t.Errorf(...) }", "assert.Error(t, err)", "if err { t.Fail() }", "try-catch dalam test"],
      "options_en": ["if (err != nil) != tt.wantErr { t.Errorf(...) }", "assert.Error(t, err)", "if err { t.Fail() }", "try-catch in test"],
      "correct": 0
    }
  ]
}
