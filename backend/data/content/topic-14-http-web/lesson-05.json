{
  "id": "05",
  "title_id": "JSON REST API Mini Project",
  "title_en": "JSON REST API Mini Project",
  "estimatedMinutes": 10,
  "content_id": "## Membangun REST API\n\nDalam lesson ini kita membangun REST API lengkap dengan CRUD operations menggunakan in-memory storage.\n\n## Struktur Proyek\n\n```\napi/\n├── main.go\n└── handler/\n    └── todo.go\n```\n\n## Data Model & Storage\n\n```go\npackage main\n\nimport (\n\t\"encoding/json\"\n\t\"net/http\"\n\t\"strconv\"\n\t\"sync\"\n)\n\ntype Todo struct {\n\tID        int    `json:\"id\"`\n\tTitle     string `json:\"title\"`\n\tCompleted bool   `json:\"completed\"`\n}\n\n// In-memory store\nvar (\n\tmu     sync.RWMutex\n\ttodos  = map[int]*Todo{}\n\tnextID = 1\n)\n```\n\n## Handler Implementasi\n\n```go\n// GET /todos — list all\nfunc listTodos(w http.ResponseWriter, r *http.Request) {\n\tmu.RLock()\n\tdefer mu.RUnlock()\n\n\tresult := make([]*Todo, 0, len(todos))\n\tfor _, t := range todos {\n\t\tresult = append(result, t)\n\t}\n\n\tw.Header().Set(\"Content-Type\", \"application/json\")\n\tjson.NewEncoder(w).Encode(result)\n}\n\n// POST /todos — create\nfunc createTodo(w http.ResponseWriter, r *http.Request) {\n\tvar input struct {\n\t\tTitle string `json:\"title\"`\n\t}\n\tif err := json.NewDecoder(r.Body).Decode(&input); err != nil {\n\t\thttp.Error(w, err.Error(), http.StatusBadRequest)\n\t\treturn\n\t}\n\tif input.Title == \"\" {\n\t\thttp.Error(w, \"title is required\", http.StatusBadRequest)\n\t\treturn\n\t}\n\n\tmu.Lock()\n\ttodo := &Todo{ID: nextID, Title: input.Title}\n\ttodos[nextID] = todo\n\tnextID++\n\tmu.Unlock()\n\n\tw.Header().Set(\"Content-Type\", \"application/json\")\n\tw.WriteHeader(http.StatusCreated)\n\tjson.NewEncoder(w).Encode(todo)\n}\n\n// DELETE /todos/{id}\nfunc deleteTodo(w http.ResponseWriter, r *http.Request) {\n\tidStr := r.PathValue(\"id\")\n\tid, err := strconv.Atoi(idStr)\n\tif err != nil {\n\t\thttp.Error(w, \"invalid id\", http.StatusBadRequest)\n\t\treturn\n\t}\n\n\tmu.Lock()\n\tdefer mu.Unlock()\n\tif _, ok := todos[id]; !ok {\n\t\thttp.Error(w, \"not found\", http.StatusNotFound)\n\t\treturn\n\t}\n\tdelete(todos, id)\n\tw.WriteHeader(http.StatusNoContent)\n}\n\nfunc main() {\n\tmux := http.NewServeMux()\n\tmux.HandleFunc(\"GET /todos\", listTodos)\n\tmux.HandleFunc(\"POST /todos\", createTodo)\n\tmux.HandleFunc(\"DELETE /todos/{id}\", deleteTodo)\n\n\thttp.ListenAndServe(\":8080\", mux)\n}\n```\n\n## Testing dengan curl\n\n```bash\n# List todos\ncurl localhost:8080/todos\n\n# Create todo\ncurl -X POST localhost:8080/todos -d '{\"title\":\"Learn Go\"}' -H 'Content-Type: application/json'\n\n# Delete todo\ncurl -X DELETE localhost:8080/todos/1\n```",
  "content_en": "## Building a REST API\n\nIn this lesson we build a complete REST API with CRUD operations using in-memory storage.\n\n## Project Structure\n\n```\napi/\n├── main.go\n└── handler/\n    └── todo.go\n```\n\n## Data Model & Storage\n\n```go\npackage main\n\nimport (\n\t\"encoding/json\"\n\t\"net/http\"\n\t\"strconv\"\n\t\"sync\"\n)\n\ntype Todo struct {\n\tID        int    `json:\"id\"`\n\tTitle     string `json:\"title\"`\n\tCompleted bool   `json:\"completed\"`\n}\n\n// In-memory store\nvar (\n\tmu     sync.RWMutex\n\ttodos  = map[int]*Todo{}\n\tnextID = 1\n)\n```\n\n## Handler Implementation\n\n```go\n// GET /todos — list all\nfunc listTodos(w http.ResponseWriter, r *http.Request) {\n\tmu.RLock()\n\tdefer mu.RUnlock()\n\n\tresult := make([]*Todo, 0, len(todos))\n\tfor _, t := range todos {\n\t\tresult = append(result, t)\n\t}\n\n\tw.Header().Set(\"Content-Type\", \"application/json\")\n\tjson.NewEncoder(w).Encode(result)\n}\n\n// POST /todos — create\nfunc createTodo(w http.ResponseWriter, r *http.Request) {\n\tvar input struct {\n\t\tTitle string `json:\"title\"`\n\t}\n\tif err := json.NewDecoder(r.Body).Decode(&input); err != nil {\n\t\thttp.Error(w, err.Error(), http.StatusBadRequest)\n\t\treturn\n\t}\n\tif input.Title == \"\" {\n\t\thttp.Error(w, \"title is required\", http.StatusBadRequest)\n\t\treturn\n\t}\n\n\tmu.Lock()\n\ttodo := &Todo{ID: nextID, Title: input.Title}\n\ttodos[nextID] = todo\n\tnextID++\n\tmu.Unlock()\n\n\tw.Header().Set(\"Content-Type\", \"application/json\")\n\tw.WriteHeader(http.StatusCreated)\n\tjson.NewEncoder(w).Encode(todo)\n}\n\n// DELETE /todos/{id}\nfunc deleteTodo(w http.ResponseWriter, r *http.Request) {\n\tidStr := r.PathValue(\"id\")\n\tid, err := strconv.Atoi(idStr)\n\tif err != nil {\n\t\thttp.Error(w, \"invalid id\", http.StatusBadRequest)\n\t\treturn\n\t}\n\n\tmu.Lock()\n\tdefer mu.Unlock()\n\tif _, ok := todos[id]; !ok {\n\t\thttp.Error(w, \"not found\", http.StatusNotFound)\n\t\treturn\n\t}\n\tdelete(todos, id)\n\tw.WriteHeader(http.StatusNoContent)\n}\n\nfunc main() {\n\tmux := http.NewServeMux()\n\tmux.HandleFunc(\"GET /todos\", listTodos)\n\tmux.HandleFunc(\"POST /todos\", createTodo)\n\tmux.HandleFunc(\"DELETE /todos/{id}\", deleteTodo)\n\n\thttp.ListenAndServe(\":8080\", mux)\n}\n```\n\n## Testing with curl\n\n```bash\n# List todos\ncurl localhost:8080/todos\n\n# Create todo\ncurl -X POST localhost:8080/todos -d '{\"title\":\"Learn Go\"}' -H 'Content-Type: application/json'\n\n# Delete todo\ncurl -X DELETE localhost:8080/todos/1\n```",
  "starterCode": "package main\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"net/http\"\n\t\"strconv\"\n\t\"sync\"\n)\n\ntype Product struct {\n\tID    int     `json:\"id\"`\n\tName  string  `json:\"name\"`\n\tPrice float64 `json:\"price\"`\n}\n\nvar (\n\tmu       sync.RWMutex\n\tproducts = map[int]*Product{}\n\tnextID   = 1\n)\n\nfunc writeJSON(w http.ResponseWriter, status int, v any) {\n\tw.Header().Set(\"Content-Type\", \"application/json\")\n\tw.WriteHeader(status)\n\tjson.NewEncoder(w).Encode(v)\n}\n\n// TODO: implement listProducts — GET /products\nfunc listProducts(w http.ResponseWriter, r *http.Request) {\n\tmu.RLock()\n\tdefer mu.RUnlock()\n\tresult := make([]*Product, 0, len(products))\n\tfor _, p := range products {\n\t\tresult = append(result, p)\n\t}\n\twriteJSON(w, http.StatusOK, result)\n}\n\n// TODO: implement createProduct — POST /products\nfunc createProduct(w http.ResponseWriter, r *http.Request) {\n\tvar p Product\n\tif err := json.NewDecoder(r.Body).Decode(&p); err != nil {\n\t\thttp.Error(w, err.Error(), http.StatusBadRequest)\n\t\treturn\n\t}\n\tmu.Lock()\n\tp.ID = nextID\n\tproducts[nextID] = &p\n\tnextID++\n\tmu.Unlock()\n\twriteJSON(w, http.StatusCreated, p)\n}\n\n// TODO: implement getProduct — GET /products/{id}\nfunc getProduct(w http.ResponseWriter, r *http.Request) {\n\tid, err := strconv.Atoi(r.PathValue(\"id\"))\n\tif err != nil {\n\t\thttp.Error(w, \"invalid id\", http.StatusBadRequest)\n\t\treturn\n\t}\n\tmu.RLock()\n\tdefer mu.RUnlock()\n\tp, ok := products[id]\n\tif !ok {\n\t\thttp.Error(w, \"not found\", http.StatusNotFound)\n\t\treturn\n\t}\n\twriteJSON(w, http.StatusOK, p)\n}\n\nfunc main() {\n\tmux := http.NewServeMux()\n\tmux.HandleFunc(\"GET /products\", listProducts)\n\tmux.HandleFunc(\"POST /products\", createProduct)\n\tmux.HandleFunc(\"GET /products/{id}\", getProduct)\n\n\tfmt.Println(\"Products API on :8080\")\n\thttp.ListenAndServe(\":8080\", mux)\n}",
  "quiz": [
    {
      "question_id": "HTTP status code yang tepat untuk resource yang berhasil dibuat (POST) adalah?",
      "question_en": "The appropriate HTTP status code for a successfully created resource (POST) is?",
      "options_id": ["201 Created", "200 OK", "204 No Content", "202 Accepted"],
      "options_en": ["201 Created", "200 OK", "204 No Content", "202 Accepted"],
      "correct": 0
    },
    {
      "question_id": "Mengapa in-memory store dalam REST API harus menggunakan sync.RWMutex?",
      "question_en": "Why should an in-memory store in a REST API use sync.RWMutex?",
      "options_id": ["Karena Go HTTP server menangani setiap request di goroutine tersendiri", "Karena map tidak thread-safe secara default di Go", "Keduanya benar", "Karena JSON encoding memerlukan lock"],
      "options_en": ["Because Go HTTP server handles each request in its own goroutine", "Because maps are not thread-safe by default in Go", "Both are correct", "Because JSON encoding requires a lock"],
      "correct": 2
    },
    {
      "question_id": "HTTP status code yang tepat untuk DELETE yang berhasil (tanpa body) adalah?",
      "question_en": "The appropriate HTTP status code for a successful DELETE (no body) is?",
      "options_id": ["204 No Content", "200 OK", "202 Accepted", "201 Created"],
      "options_en": ["204 No Content", "200 OK", "202 Accepted", "201 Created"],
      "correct": 0
    }
  ]
}
