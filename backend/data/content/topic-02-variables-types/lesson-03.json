{
  "id": "03",
  "title_id": "Zero Values & Multiple Assignment",
  "title_en": "Zero Values & Multiple Assignment",
  "estimatedMinutes": 8,
  "content_id": "# Zero Values & Multiple Assignment\n\n## Zero Values\n\nDi Go, setiap variabel yang dideklarasikan tanpa nilai awal akan secara otomatis mendapatkan **zero value** sesuai tipenya. Ini menghilangkan masalah variabel yang tidak terinisialisasi.\n\n| Tipe | Zero Value |\n|---|---|\n| `int`, `int8`, ... `int64` | `0` |\n| `uint`, `uint8`, ... `uint64` | `0` |\n| `float32`, `float64` | `0.0` atau `0` |\n| `bool` | `false` |\n| `string` | `\"\"` (string kosong) |\n| `pointer` | `nil` |\n| `slice` | `nil` |\n| `map` | `nil` |\n| `channel` | `nil` |\n| `interface` | `nil` |\n| `func` | `nil` |\n\n### Contoh Zero Values\n\n```go\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n    var i int\n    var f float64\n    var b bool\n    var s string\n\n    fmt.Printf(\"int zero: %d\\n\", i)       // 0\n    fmt.Printf(\"float zero: %f\\n\", f)     // 0.000000\n    fmt.Printf(\"bool zero: %t\\n\", b)      // false\n    fmt.Printf(\"string zero: %q\\n\", s)    // \"\"\n}\n```\n\n## Multiple Assignment\n\nGo mendukung assignment banyak variabel sekaligus dalam satu baris:\n\n```go\n// Deklarasi dan inisialisasi banyak variabel\nx, y, z := 1, 2, 3\nfmt.Println(x, y, z) // 1 2 3\n\n// Assignment ulang\na, b := 10, 20\na, b = 100, 200\nfmt.Println(a, b) // 100 200\n```\n\n### Swap Trick Idiomatik\n\n```go\na, b := 5, 10\nfmt.Println(\"Sebelum swap:\", a, b) // 5 10\n\na, b = b, a  // swap dalam satu baris!\nfmt.Println(\"Setelah swap:\", a, b) // 10 5\n```\n\nTanpa variabel temporary! Ini adalah salah satu keunggulan Go.\n\n## Blank Identifier `_`\n\nBlank identifier (`_`) digunakan untuk mengabaikan nilai yang tidak diperlukan:\n\n```go\n// Mengabaikan nilai return yang tidak dibutuhkan\nfunc getCoords() (int, int) {\n    return 10, 20\n}\n\nfunc main() {\n    // Hanya butuh y, abaikan x\n    _, y := getCoords()\n    fmt.Println(\"y:\", y) // y: 20\n\n    // Abaikan index saat range\n    nums := []int{1, 2, 3, 4, 5}\n    for _, v := range nums {\n        fmt.Print(v, \" \") // 1 2 3 4 5\n    }\n}\n```\n\n## Aturan Variabel Tidak Terpakai\n\nGo **memaksa** setiap variabel yang dideklarasikan untuk digunakan. Jika tidak, program tidak akan bisa dikompilasi:\n\n```go\nfunc main() {\n    x := 5  // ERROR jika x tidak pernah digunakan!\n    // compile error: x declared and not used\n}\n```\n\nIni berbeda dengan Python atau JavaScript yang memperbolehkan variabel tidak terpakai. Aturan ini membantu menjaga kode tetap bersih.",
  "content_en": "# Zero Values & Multiple Assignment\n\n## Zero Values\n\nIn Go, every variable declared without an initial value automatically receives a **zero value** based on its type. This eliminates the problem of uninitialized variables.\n\n| Type | Zero Value |\n|---|---|\n| `int`, `int8`, ... `int64` | `0` |\n| `uint`, `uint8`, ... `uint64` | `0` |\n| `float32`, `float64` | `0.0` or `0` |\n| `bool` | `false` |\n| `string` | `\"\"` (empty string) |\n| `pointer` | `nil` |\n| `slice` | `nil` |\n| `map` | `nil` |\n| `channel` | `nil` |\n| `interface` | `nil` |\n| `func` | `nil` |\n\n### Zero Value Example\n\n```go\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n    var i int\n    var f float64\n    var b bool\n    var s string\n\n    fmt.Printf(\"int zero: %d\\n\", i)       // 0\n    fmt.Printf(\"float zero: %f\\n\", f)     // 0.000000\n    fmt.Printf(\"bool zero: %t\\n\", b)      // false\n    fmt.Printf(\"string zero: %q\\n\", s)    // \"\"\n}\n```\n\n## Multiple Assignment\n\nGo supports assigning multiple variables at once in one line:\n\n```go\n// Declare and initialize multiple variables\nx, y, z := 1, 2, 3\nfmt.Println(x, y, z) // 1 2 3\n\n// Re-assignment\na, b := 10, 20\na, b = 100, 200\nfmt.Println(a, b) // 100 200\n```\n\n### The Idiomatic Swap Trick\n\n```go\na, b := 5, 10\nfmt.Println(\"Before swap:\", a, b) // 5 10\n\na, b = b, a  // swap in one line!\nfmt.Println(\"After swap:\", a, b) // 10 5\n```\n\nNo temporary variable needed! This is one of Go's conveniences.\n\n## Blank Identifier `_`\n\nThe blank identifier (`_`) is used to discard values you don't need:\n\n```go\n// Ignore return values that aren't needed\nfunc getCoords() (int, int) {\n    return 10, 20\n}\n\nfunc main() {\n    // Only need y, ignore x\n    _, y := getCoords()\n    fmt.Println(\"y:\", y) // y: 20\n\n    // Ignore index during range\n    nums := []int{1, 2, 3, 4, 5}\n    for _, v := range nums {\n        fmt.Print(v, \" \") // 1 2 3 4 5\n    }\n}\n```\n\n## Unused Variable Rule\n\nGo **enforces** that every declared variable must be used. If not, the program won't compile:\n\n```go\nfunc main() {\n    x := 5  // ERROR if x is never used!\n    // compile error: x declared and not used\n}\n```\n\nThis differs from Python or JavaScript which allow unused variables. This rule helps keep code clean.",
  "starterCode": "package main\n\nimport \"fmt\"\n\nfunc getPoint() (int, int) {\n\treturn 42, 100\n}\n\nfunc main() {\n\t// Zero values\n\tvar i int\n\tvar f float64\n\tvar b bool\n\tvar s string\n\tfmt.Printf(\"Zero values: int=%d, float=%f, bool=%t, string=%q\\n\", i, f, b, s)\n\n\t// Multiple assignment\n\tx, y, z := 10, 20, 30\n\tfmt.Println(\"x, y, z:\", x, y, z)\n\n\t// Swap trick\n\tx, y = y, x\n\tfmt.Println(\"Setelah swap x,y:\", x, y)\n\n\t// Blank identifier\n\t_, coordY := getPoint()\n\tfmt.Println(\"Hanya y:\", coordY)\n\n\t_ = z // gunakan z agar tidak error\n}",
  "quiz": [
    {
      "question_id": "Pasangan tipe dan zero value mana yang benar menurut lesson?",
      "question_en": "Which type and zero-value pair is correct according to the lesson?",
      "options_id": [
        "`bool` -> `true`",
        "`string` -> `nil`",
        "`map` -> `nil`",
        "`int` -> `undefined`"
      ],
      "options_en": [
        "`bool` -> `true`",
        "`string` -> `nil`",
        "`map` -> `nil`",
        "`int` -> `undefined`"
      ],
      "correct": 2
    },
    {
      "question_id": "Jika `var s string` dideklarasikan tanpa nilai awal, apa representasi output `%q` untuk `s`?",
      "question_en": "If `var s string` is declared with no initial value, what `%q` output represents `s`?",
      "options_id": [
        "`nil`",
        "`\"\"`",
        "`0`",
        "`undefined`"
      ],
      "options_en": [
        "`nil`",
        "`\"\"`",
        "`0`",
        "`undefined`"
      ],
      "correct": 1
    },
    {
      "question_id": "Apa manfaat utama konsep zero value di Go menurut lesson?",
      "question_en": "What is the main benefit of the zero-value concept in Go according to the lesson?",
      "options_id": [
        "Menghindari masalah variabel yang tidak terinisialisasi",
        "Membuat semua variabel otomatis menjadi konstanta",
        "Menghilangkan kebutuhan tipe data",
        "Mewajibkan semua variabel bernilai `nil`"
      ],
      "options_en": [
        "It avoids the problem of uninitialized variables",
        "It makes all variables automatically become constants",
        "It removes the need for data types",
        "It requires all variables to be `nil`"
      ],
      "correct": 0
    },
    {
      "question_id": "Fitur apa yang ditunjukkan oleh `x, y, z := 1, 2, 3`?",
      "question_en": "What feature is demonstrated by `x, y, z := 1, 2, 3`?",
      "options_id": [
        "Named return values",
        "Multiple declaration/assignment in one line",
        "Const grouping",
        "Type conversion"
      ],
      "options_en": [
        "Named return values",
        "Multiple declaration/assignment in one line",
        "Constant grouping",
        "Type conversion"
      ],
      "correct": 1
    },
    {
      "question_id": "Mengapa `a, b = b, a` dianggap idiomatik pada lesson ini?",
      "question_en": "Why is `a, b = b, a` considered idiomatic in this lesson?",
      "options_id": [
        "Karena menukar nilai dalam satu baris tanpa variabel temporary",
        "Karena hanya bekerja untuk string",
        "Karena menghasilkan nilai zero value sementara",
        "Karena mengubah `a` dan `b` menjadi konstanta"
      ],
      "options_en": [
        "Because it swaps values in one line without a temporary variable",
        "Because it only works for strings",
        "Because it creates temporary zero values",
        "Because it turns `a` and `b` into constants"
      ],
      "correct": 0
    },
    {
      "question_id": "Apa fungsi blank identifier `_` pada `_, y := getCoords()`?",
      "question_en": "What does the blank identifier `_` do in `_, y := getCoords()`?",
      "options_id": [
        "Mengonversi nilai pertama menjadi `nil`",
        "Mengabaikan nilai pertama yang tidak dibutuhkan",
        "Menjadikan `y` package-level",
        "Memaksa fungsi hanya mengembalikan satu nilai"
      ],
      "options_en": [
        "It converts the first value to `nil`",
        "It discards the first value that is not needed",
        "It makes `y` package-level",
        "It forces the function to return only one value"
      ],
      "correct": 1
    },
    {
      "question_id": "Pada `for _, v := range nums`, mengapa `_` dipilih?",
      "question_en": "In `for _, v := range nums`, why is `_` used?",
      "options_id": [
        "Karena hanya nilai elemen yang dipakai, bukan index",
        "Karena `range` selalu butuh tiga variabel",
        "Karena `v` harus berupa pointer",
        "Karena index array di Go selalu private"
      ],
      "options_en": [
        "Because only the element value is needed, not the index",
        "Because `range` always needs three variables",
        "Because `v` must be a pointer",
        "Because array indexes in Go are always private"
      ],
      "correct": 0
    },
    {
      "question_id": "Apa yang terjadi jika variabel lokal dideklarasikan tetapi tidak pernah digunakan?",
      "question_en": "What happens if a local variable is declared but never used?",
      "options_id": [
        "Program tetap berjalan dengan warning",
        "Compiler mengabaikannya secara diam-diam",
        "Kompilasi gagal dengan error",
        "Variabel otomatis dihapus saat runtime"
      ],
      "options_en": [
        "The program still runs with a warning",
        "The compiler silently ignores it",
        "Compilation fails with an error",
        "The variable is automatically removed at runtime"
      ],
      "correct": 2
    },
    {
      "question_id": "Contoh mana yang paling tepat menunjukkan zero value bertipe referensi dari tabel?",
      "question_en": "Which example best shows a reference-like zero value from the table?",
      "options_id": [
        "`var n int` menghasilkan `0`",
        "`var ok bool` menghasilkan `false`",
        "`var m map[string]int` menghasilkan `nil`",
        "`var s string` menghasilkan `\"go\"`"
      ],
      "options_en": [
        "`var n int` yields `0`",
        "`var ok bool` yields `false`",
        "`var m map[string]int` yields `nil`",
        "`var s string` yields `\"go\"`"
      ],
      "correct": 2
    },
    {
      "question_id": "Mengapa starter code menulis `_ = z` di akhir?",
      "question_en": "Why does the starter code include `_ = z` at the end?",
      "options_id": [
        "Untuk menghindari error variabel tidak digunakan",
        "Untuk mereset `z` ke zero value",
        "Untuk mengubah `z` menjadi konstanta",
        "Untuk membuat `z` bisa diakses global"
      ],
      "options_en": [
        "To avoid an unused-variable error",
        "To reset `z` to its zero value",
        "To turn `z` into a constant",
        "To make `z` globally accessible"
      ],
      "correct": 0
    }
  ]
}
