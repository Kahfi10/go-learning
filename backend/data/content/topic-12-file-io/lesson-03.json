{
  "id": "03",
  "title_id": "JSON Encoding & Decoding",
  "title_en": "JSON Encoding & Decoding",
  "estimatedMinutes": 10,
  "content_id": "## Package encoding/json\n\nGo memiliki dukungan JSON built-in yang sangat baik melalui package `encoding/json`.\n\n## Marshal: Go → JSON\n\n`json.Marshal()` mengubah struct Go menjadi JSON:\n\n```go\npackage main\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n)\n\ntype Person struct {\n\tName    string `json:\"name\"`\n\tAge     int    `json:\"age\"`\n\tEmail   string `json:\"email,omitempty\"` // omit jika kosong\n\tpassword string // unexported — diabaikan oleh JSON\n}\n\nfunc main() {\n\tp := Person{Name: \"Alice\", Age: 30, Email: \"alice@example.com\"}\n\n\tdata, err := json.Marshal(p)\n\tif err != nil {\n\t\tfmt.Println(\"Error:\", err)\n\t\treturn\n\t}\n\tfmt.Println(string(data))\n\t// {\"name\":\"Alice\",\"age\":30,\"email\":\"alice@example.com\"}\n\n\t// Pretty print\n\tpretty, _ := json.MarshalIndent(p, \"\", \"  \")\n\tfmt.Println(string(pretty))\n}\n```\n\n## Unmarshal: JSON → Go\n\n`json.Unmarshal()` mengurai JSON ke struct Go:\n\n```go\njsonStr := `{\"name\":\"Bob\",\"age\":25,\"email\":\"bob@example.com\"}`\n\nvar p Person\nif err := json.Unmarshal([]byte(jsonStr), &p); err != nil {\n\tfmt.Println(\"Error:\", err)\n\treturn\n}\nfmt.Printf(\"%+v\\n\", p) // {Name:Bob Age:25 Email:bob@example.com password:}\n```\n\n## Struct Tags JSON\n\n```go\ntype Product struct {\n\tID       int    `json:\"id\"`            // rename field\n\tName     string `json:\"name\"`\n\tPrice    float64 `json:\"price\"`\n\tInStock  bool   `json:\"in_stock\"`\n\tInternal string `json:\"-\"`             // selalu diabaikan\n\tNotes    string `json:\"notes,omitempty\"` // diabaikan jika zero value\n}\n```\n\n## Streaming JSON dengan Encoder/Decoder\n\nUntuk file besar atau HTTP, gunakan `json.NewEncoder`/`json.NewDecoder`:\n\n```go\n// Encode ke file\nfile, _ := os.Create(\"data.json\")\ndefer file.Close()\nencoder := json.NewEncoder(file)\nencoder.SetIndent(\"\", \"  \")\nencoder.Encode(p) // tidak butuh json.Marshal\n\n// Decode dari HTTP response\nresp, _ := http.Get(\"https://api.example.com/user\")\ndefer resp.Body.Close()\nvar user User\njson.NewDecoder(resp.Body).Decode(&user)\n```",
  "content_en": "## The encoding/json Package\n\nGo has excellent built-in JSON support through the `encoding/json` package.\n\n## Marshal: Go → JSON\n\n`json.Marshal()` converts a Go struct to JSON:\n\n```go\npackage main\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n)\n\ntype Person struct {\n\tName    string `json:\"name\"`\n\tAge     int    `json:\"age\"`\n\tEmail   string `json:\"email,omitempty\"` // omit if empty\n\tpassword string // unexported — ignored by JSON\n}\n\nfunc main() {\n\tp := Person{Name: \"Alice\", Age: 30, Email: \"alice@example.com\"}\n\n\tdata, err := json.Marshal(p)\n\tif err != nil {\n\t\tfmt.Println(\"Error:\", err)\n\t\treturn\n\t}\n\tfmt.Println(string(data))\n\t// {\"name\":\"Alice\",\"age\":30,\"email\":\"alice@example.com\"}\n\n\t// Pretty print\n\tpretty, _ := json.MarshalIndent(p, \"\", \"  \")\n\tfmt.Println(string(pretty))\n}\n```\n\n## Unmarshal: JSON → Go\n\n`json.Unmarshal()` parses JSON into a Go struct:\n\n```go\njsonStr := `{\"name\":\"Bob\",\"age\":25,\"email\":\"bob@example.com\"}`\n\nvar p Person\nif err := json.Unmarshal([]byte(jsonStr), &p); err != nil {\n\tfmt.Println(\"Error:\", err)\n\treturn\n}\nfmt.Printf(\"%+v\\n\", p) // {Name:Bob Age:25 Email:bob@example.com password:}\n```\n\n## JSON Struct Tags\n\n```go\ntype Product struct {\n\tID       int     `json:\"id\"`              // rename field\n\tName     string  `json:\"name\"`\n\tPrice    float64 `json:\"price\"`\n\tInStock  bool    `json:\"in_stock\"`\n\tInternal string  `json:\"-\"`              // always ignored\n\tNotes    string  `json:\"notes,omitempty\"` // ignored if zero value\n}\n```\n\n## Streaming JSON with Encoder/Decoder\n\nFor large files or HTTP, use `json.NewEncoder`/`json.NewDecoder`:\n\n```go\n// Encode to file\nfile, _ := os.Create(\"data.json\")\ndefer file.Close()\nencoder := json.NewEncoder(file)\nencoder.SetIndent(\"\", \"  \")\nencoder.Encode(p) // no need for json.Marshal\n\n// Decode from HTTP response\nresp, _ := http.Get(\"https://api.example.com/user\")\ndefer resp.Body.Close()\nvar user User\njson.NewDecoder(resp.Body).Decode(&user)\n```",
  "starterCode": "package main\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n)\n\ntype Movie struct {\n\t// TODO: add fields: Title (string), Year (int), Rating (float64), Director (string)\n\t// Add proper json struct tags\n\tTitle    string\n\tYear     int\n\tRating   float64\n\tDirector string\n}\n\nfunc main() {\n\t// TODO: create a Movie struct with values and marshal it to JSON\n\tmovie := Movie{\n\t\tTitle:    \"The Go Gopher Movie\",\n\t\tYear:     2024,\n\t\tRating:   9.5,\n\t\tDirector: \"Rob Pike\",\n\t}\n\n\t// Marshal to JSON\n\tdata, err := json.Marshal(movie)\n\tif err != nil {\n\t\tfmt.Println(\"Marshal error:\", err)\n\t\treturn\n\t}\n\tfmt.Println(string(data))\n\n\t// TODO: unmarshal the JSON back into a new Movie struct and print it\n\tvar decoded Movie\n\tif err := json.Unmarshal(data, &decoded); err != nil {\n\t\tfmt.Println(\"Unmarshal error:\", err)\n\t\treturn\n\t}\n\tfmt.Printf(\"%+v\\n\", decoded)\n}",
  "quiz": [
    {
      "question_id": "Fungsi apa yang mengubah struct Go menjadi JSON?",
      "question_en": "Which function converts a Go struct to JSON?",
      "options_id": ["json.Marshal()", "json.Encode()", "json.Serialize()", "json.Convert()"],
      "options_en": ["json.Marshal()", "json.Encode()", "json.Serialize()", "json.Convert()"],
      "correct": 0
    },
    {
      "question_id": "Apa efek dari tag `json:\"name,omitempty\"` pada sebuah field?",
      "question_en": "What does the tag `json:\"name,omitempty\"` do to a field?",
      "options_id": ["Field diabaikan dalam JSON jika nilainya zero value", "Field selalu disertakan dalam JSON", "Field dienkripsi dalam JSON", "Field diganti namanya menjadi 'omitempty'"],
      "options_en": ["The field is omitted from JSON if its value is the zero value", "The field is always included in JSON", "The field is encrypted in JSON", "The field is renamed to 'omitempty'"],
      "correct": 0
    },
    {
      "question_id": "Mengapa json.NewDecoder lebih baik dari json.Unmarshal untuk HTTP response?",
      "question_en": "Why is json.NewDecoder better than json.Unmarshal for HTTP responses?",
      "options_id": ["Bisa decode langsung dari io.Reader tanpa membaca semua data ke memori", "Lebih cepat selalu", "Tidak membutuhkan struct tag", "Menghandle error lebih baik"],
      "options_en": ["Can decode directly from io.Reader without loading all data into memory", "Always faster", "Doesn't require struct tags", "Handles errors better"],
      "correct": 0
    }
  ]
}
