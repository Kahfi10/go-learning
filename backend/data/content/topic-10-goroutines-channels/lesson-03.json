{
  "id": "03",
  "title_id": "Buffered Channels",
  "title_en": "Buffered Channels",
  "estimatedMinutes": 10,
  "content_id": "## Buffered Channel\n\nBerbeda dengan unbuffered channel, **buffered channel** memiliki kapasitas internal. Send hanya memblokir ketika buffer **penuh**, dan receive hanya memblokir ketika buffer **kosong**.\n\n## Membuat Buffered Channel\n\n```go\nch := make(chan int, 3) // buffer berkapasitas 3\n```\n\n## Perilaku Non-Blocking\n\n```go\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n\tch := make(chan string, 2)\n\n\t// Ini TIDAK memblokir karena buffer masih ada tempat\n\tch <- \"ping\"\n\tch <- \"pong\"\n\n\t// Buffer penuh — send berikutnya akan memblokir!\n\t// ch <- \"overflow\" // ini akan deadlock jika tidak ada goroutine lain\n\n\tfmt.Println(<-ch) // ping\n\tfmt.Println(<-ch) // pong\n}\n```\n\n## Pattern: Semaphore dengan Buffered Channel\n\nBuffered channel sering digunakan sebagai semaphore untuk membatasi concurrency:\n\n```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"time\"\n)\n\nfunc main() {\n\t// Batasi maksimal 3 goroutine berjalan bersamaan\n\tsem := make(chan struct{}, 3)\n\n\tfor i := 0; i < 10; i++ {\n\t\ti := i\n\t\tsem <- struct{}{} // acquire slot\n\t\tgo func() {\n\t\t\tdefer func() { <-sem }() // release slot\n\t\t\tfmt.Printf(\"worker %d running\\n\", i)\n\t\t\ttime.Sleep(100 * time.Millisecond)\n\t\t}()\n\t}\n\n\t// Tunggu semua selesai\n\tfor i := 0; i < cap(sem); i++ {\n\t\tsem <- struct{}{}\n\t}\n}\n```\n\n## len dan cap pada Channel\n\n```go\nch := make(chan int, 5)\nch <- 1\nch <- 2\nch <- 3\n\nfmt.Println(len(ch)) // 3 — jumlah elemen saat ini\nfmt.Println(cap(ch)) // 5 — kapasitas total buffer\n```\n\n## Kapan Gunakan Buffered?\n\n| Situasi | Pilihan |\n|---|---|\n| Sinkronisasi ketat (rendezvous) | Unbuffered |\n| Decoupling producer-consumer | Buffered |\n| Rate limiting / semaphore | Buffered |\n| Batch processing | Buffered |",
  "content_en": "## Buffered Channel\n\nUnlike unbuffered channels, a **buffered channel** has internal capacity. Send only blocks when the buffer is **full**, and receive only blocks when the buffer is **empty**.\n\n## Creating a Buffered Channel\n\n```go\nch := make(chan int, 3) // buffer with capacity 3\n```\n\n## Non-Blocking Behavior\n\n```go\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n\tch := make(chan string, 2)\n\n\t// These do NOT block because buffer has space\n\tch <- \"ping\"\n\tch <- \"pong\"\n\n\t// Buffer is full — the next send would block!\n\t// ch <- \"overflow\" // would deadlock without another goroutine\n\n\tfmt.Println(<-ch) // ping\n\tfmt.Println(<-ch) // pong\n}\n```\n\n## Pattern: Semaphore with Buffered Channel\n\nBuffered channels are often used as semaphores to limit concurrency:\n\n```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"time\"\n)\n\nfunc main() {\n\t// Limit to max 3 goroutines running simultaneously\n\tsem := make(chan struct{}, 3)\n\n\tfor i := 0; i < 10; i++ {\n\t\ti := i\n\t\tsem <- struct{}{} // acquire slot\n\t\tgo func() {\n\t\t\tdefer func() { <-sem }() // release slot\n\t\t\tfmt.Printf(\"worker %d running\\n\", i)\n\t\t\ttime.Sleep(100 * time.Millisecond)\n\t\t}()\n\t}\n\n\t// Wait for all to finish\n\tfor i := 0; i < cap(sem); i++ {\n\t\tsem <- struct{}{}\n\t}\n}\n```\n\n## len and cap on Channels\n\n```go\nch := make(chan int, 5)\nch <- 1\nch <- 2\nch <- 3\n\nfmt.Println(len(ch)) // 3 — current number of elements\nfmt.Println(cap(ch)) // 5 — total buffer capacity\n```\n\n## When to Use Buffered?\n\n| Situation | Choice |\n|---|---|\n| Strict synchronization (rendezvous) | Unbuffered |\n| Decoupling producer-consumer | Buffered |\n| Rate limiting / semaphore | Buffered |\n| Batch processing | Buffered |",
  "starterCode": "package main\n\nimport (\n\t\"fmt\"\n\t\"time\"\n)\n\nfunc producer(jobs chan<- int, n int) {\n\tfor i := 1; i <= n; i++ {\n\t\tfmt.Println(\"producing job\", i)\n\t\tjobs <- i\n\t}\n\tclose(jobs)\n}\n\nfunc consumer(jobs <-chan int) {\n\tfor job := range jobs {\n\t\ttime.Sleep(50 * time.Millisecond)\n\t\tfmt.Println(\"consumed job\", job)\n\t}\n}\n\nfunc main() {\n\t// TODO: create a buffered channel with capacity 3\n\tjobs := make(chan int)\n\n\tgo producer(jobs, 5)\n\tconsumer(jobs)\n\n\tfmt.Println(\"all jobs done\")\n}",
  "quiz": [
    {
      "question_id": "Apa arti `make(chan string, 2)`?",
      "question_en": "What does `make(chan string, 2)` mean?",
      "options_id": ["Membuat buffered channel `string` dengan kapasitas 2", "Membuat unbuffered channel yang sudah berisi 2 string kosong", "Membuat array berisi 2 channel string", "Membuat channel yang hanya boleh di-receive dua kali"],
      "options_en": ["It creates a buffered `string` channel with capacity 2", "It creates an unbuffered channel already containing 2 empty strings", "It creates an array of 2 string channels", "It creates a channel that may only be received from twice"],
      "correct": 0
    },
    {
      "question_id": "Pada contoh `ch <- \"ping\"; ch <- \"pong\";`, apa yang membuat kedua send pertama tidak memblokir?",
      "question_en": "In the example `ch <- \"ping\"; ch <- \"pong\";`, what allows the first two sends to avoid blocking?",
      "options_id": ["Buffer masih memiliki slot kosong untuk menampung nilainya", "Go otomatis membuat goroutine receiver tersembunyi", "`len(ch)` selalu nol pada buffered channel", "Setiap dua send pertama selalu non-blocking apa pun kapasitasnya"],
      "options_en": ["The buffer still has free slots to hold the values", "Go automatically creates a hidden receiver goroutine", "`len(ch)` is always zero on a buffered channel", "The first two sends are always non-blocking regardless of capacity"],
      "correct": 0
    },
    {
      "question_id": "Kapan send ke buffered channel mulai memblokir?",
      "question_en": "When does a send to a buffered channel start blocking?",
      "options_id": ["Saat buffer kosong", "Setelah receive pertama terjadi", "Saat buffer penuh", "Tidak pernah pada buffered channel"],
      "options_en": ["When the buffer is empty", "After the first receive happens", "When the buffer is full", "Never on a buffered channel"],
      "correct": 2
    },
    {
      "question_id": "Kapan receive dari buffered channel memblokir?",
      "question_en": "When does a receive from a buffered channel block?",
      "options_id": ["Saat buffer kosong", "Saat buffer penuh", "Setelah satu send berhasil", "Receive tidak pernah memblokir pada buffered channel"],
      "options_en": ["When the buffer is empty", "When the buffer is full", "After one send succeeds", "A receive never blocks on a buffered channel"],
      "correct": 0
    },
    {
      "question_id": "Jika `len(ch) == 3` dan `cap(ch) == 5`, berapa banyak nilai lagi yang bisa dikirim tanpa receiver tambahan?",
      "question_en": "If `len(ch) == 3` and `cap(ch) == 5`, how many more values can be sent without an additional receiver?",
      "options_id": ["5", "3", "2", "0"],
      "options_en": ["5", "3", "2", "0"],
      "correct": 2
    },
    {
      "question_id": "Apa yang dikembalikan `cap(ch)` pada buffered channel?",
      "question_en": "What does `cap(ch)` return for a buffered channel?",
      "options_id": ["Jumlah goroutine yang menunggu", "Kapasitas total buffer channel", "Jumlah byte per elemen", "Jumlah receive yang tersisa"],
      "options_en": ["The number of waiting goroutines", "The total capacity of the channel buffer", "The number of bytes per element", "The number of remaining receives"],
      "correct": 1
    },
    {
      "question_id": "Dalam pattern semaphore, tujuan `sem <- struct{}{}` sebelum meluncurkan worker adalah?",
      "question_en": "In the semaphore pattern, what is the purpose of `sem <- struct{}{}` before launching a worker?",
      "options_id": ["Mengambil satu slot semaphore sehingga jumlah worker concurrent dibatasi", "Menutup channel saat worker dimulai", "Mengosongkan buffer sebelum goroutine dibuat", "Menyalin data ke worker"],
      "options_en": ["To acquire one semaphore slot so concurrent workers are limited", "To close the channel when the worker starts", "To empty the buffer before the goroutine is created", "To copy data into the worker"],
      "correct": 0
    },
    {
      "question_id": "Mengapa worker melakukan `defer func() { <-sem }()`?",
      "question_en": "Why does the worker use `defer func() { <-sem }()`?",
      "options_id": ["Untuk menambah kapasitas channel satu slot", "Untuk melepas slot semaphore saat worker selesai", "Untuk menerima hasil dari worker lain", "Untuk memastikan worker menjadi goroutine utama"],
      "options_en": ["To increase the channel capacity by one slot", "To release the semaphore slot when the worker finishes", "To receive results from another worker", "To ensure the worker becomes the main goroutine"],
      "correct": 1
    },
    {
      "question_id": "Mengapa loop akhir yang mengirim `cap(sem)` token ke `sem` dapat dipakai untuk menunggu semua worker selesai?",
      "question_en": "Why can the final loop that sends `cap(sem)` tokens into `sem` be used to wait for all workers to finish?",
      "options_id": ["Karena `cap(sem)` selalu sama dengan jumlah total worker", "Karena pengiriman akhir baru bisa memenuhi semua slot setelah worker melepaskan tokennya", "Karena `sem` otomatis berubah menjadi `sync.WaitGroup`", "Karena buffered channel selalu menunggu semua goroutine saat program keluar"],
      "options_en": ["Because `cap(sem)` is always equal to the total number of workers", "Because those final sends can only fill all slots after workers release their tokens", "Because `sem` automatically becomes a `sync.WaitGroup`", "Because buffered channels always wait for all goroutines when the program exits"],
      "correct": 1
    },
    {
      "question_id": "Situasi mana dari tabel yang paling cocok memakai unbuffered channel daripada buffered?",
      "question_en": "Which situation from the table is best matched with an unbuffered channel instead of a buffered one?",
      "options_id": ["Rate limiting atau semaphore", "Batch processing", "Decoupling producer-consumer", "Sinkronisasi ketat (rendezvous)"],
      "options_en": ["Rate limiting or semaphore", "Batch processing", "Decoupling producer-consumer", "Strict synchronization (rendezvous)"],
      "correct": 3
    }
  ]
}
