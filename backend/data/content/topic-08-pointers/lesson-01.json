{
  "id": "01",
  "title_id": "Pointer Basics",
  "title_en": "Pointer Basics",
  "estimatedMinutes": 10,
  "content_id": "# Pointer Basics di Go\n\nPointer adalah variabel yang menyimpan **alamat memori** dari variabel lain. Dengan pointer, kamu bisa memanipulasi data secara langsung di lokasi memorinya tanpa membuat salinan.\n\n## Operator `&` — Address-Of\n\nOperator `&` mengambil alamat memori dari sebuah variabel:\n\n```go\nx := 42\np := &x // p adalah pointer ke x, tipenya *int\n\nfmt.Println(x)  // 42    — nilai x\nfmt.Println(p)  // 0xc0000b4010 — alamat memori x\nfmt.Println(&x) // 0xc0000b4010 — sama dengan p\nfmt.Printf(\"Alamat x: %p\\n\", p) // format %p untuk pointer\n```\n\n## Operator `*` — Dereference\n\nOperator `*` mengakses nilai pada alamat yang ditunjuk pointer:\n\n```go\nx := 42\np := &x\n\nfmt.Println(*p)  // 42 — dereference: akses nilai di alamat p\n\n// Ubah nilai melalui pointer\n*p = 100\nfmt.Println(x)   // 100 — x berubah!\nfmt.Println(*p)  // 100\n```\n\n## Tipe Pointer `*T`\n\nTipe pointer ditulis sebagai `*T` dimana T adalah tipe nilai yang ditunjuk:\n\n```go\nvar pi *int     // pointer ke int, nilai awal: nil\nvar ps *string  // pointer ke string\nvar pb *bool    // pointer ke bool\n\na := 10\nb := \"hello\"\nc := true\n\npi = &a\nps = &b\npb = &c\n\nfmt.Println(*pi, *ps, *pb) // 10 hello true\n```\n\n## Fungsi `new(T)`\n\nFungsi `new(T)` mengalokasikan memori untuk satu nilai bertipe `T`, menginisialisasinya dengan zero value, dan mengembalikan pointer:\n\n```go\n// new(T) — alokasi + zero value + return pointer\np := new(int)\nfmt.Println(*p) // 0 — zero value untuk int\n*p = 42\nfmt.Println(*p) // 42\n\np2 := new(string)\nfmt.Println(*p2 == \"\")  // true — zero value string\n*p2 = \"hello\"\nfmt.Println(*p2) // hello\n\n// new vs &T{} — biasanya &T{} lebih idiomatis untuk struct\np3 := new(int)  // sama dengan:\np4 := new(int)\n_ = p4\nvar temp int\np5 := &temp     // ekuivalen dengan new(int)\nfmt.Println(p3, p5)\n```\n\n## Zero Value Pointer: `nil`\n\nZero value untuk semua tipe pointer adalah `nil`:\n\n```go\nvar p *int\nfmt.Println(p)        // <nil>\nfmt.Println(p == nil) // true\n\n// JANGAN dereference nil pointer!\n// fmt.Println(*p) // panic: runtime error: invalid memory address or nil pointer dereference\n\n// Selalu cek nil sebelum dereference\nif p != nil {\n    fmt.Println(*p)\n} else {\n    fmt.Println(\"pointer adalah nil\")\n}\n```\n\n## Mencetak Alamat Pointer dengan `%p`\n\n```go\nx := 42\np := &x\n\nfmt.Printf(\"Nilai x: %d\\n\", x)\nfmt.Printf(\"Alamat x: %p\\n\", p)\nfmt.Printf(\"Nilai melalui pointer: %d\\n\", *p)\n\n// Perbandingan alamat dua variabel\ny := 42\nfmt.Println(&x == &y) // false — alamat berbeda meski nilai sama\n\n// Pointer ke variabel yang sama\np2 := &x\nfmt.Println(p == p2)  // true — menunjuk ke variabel yang sama\n```\n\n## Pointer Aritmatika — Tidak Ada di Go!\n\nBerbeda dari C/C++, Go **tidak mendukung pointer aritmatika** secara langsung:\n\n```go\nx := [3]int{1, 2, 3}\np := &x[0]\n// p++  // ERROR di Go — tidak bisa\n// p+1  // ERROR\n\n// Go sengaja tidak punya pointer aritmatika untuk keamanan\n// Gunakan indeks array/slice biasa\nfmt.Println(x[0], x[1], x[2])\n```\n\n## Contoh Praktis: Fungsi yang Mengubah Nilai\n\n```go\n// Tanpa pointer — tidak mengubah nilai asli\nfunc doubleVal(n int) {\n    n *= 2\n}\n\n// Dengan pointer — mengubah nilai asli\nfunc doublePtr(n *int) {\n    *n *= 2\n}\n\nx := 5\ndoubleVal(x)\nfmt.Println(x) // 5 — tidak berubah\n\ndoublePtr(&x)\nfmt.Println(x) // 10 — berubah!\n```",
  "content_en": "# Pointer Basics in Go\n\nA pointer is a variable that stores the **memory address** of another variable. With pointers, you can manipulate data directly at its memory location without making a copy.\n\n## `&` Operator — Address-Of\n\n```go\nx := 42\np := &x  // p is a pointer to x, type *int\n\nfmt.Println(x)  // 42\nfmt.Println(p)  // 0xc0000b4010 (memory address)\nfmt.Printf(\"%p\\n\", p) // use %p format verb\n```\n\n## `*` Operator — Dereference\n\n```go\nx := 42\np := &x\n\nfmt.Println(*p) // 42 — dereference: read value at address\n*p = 100        // write through pointer\nfmt.Println(x)  // 100 — x changed!\n```\n\n## Pointer Type `*T`\n\n```go\nvar pi *int    // pointer to int, zero value: nil\na := 10\npi = &a\nfmt.Println(*pi) // 10\n```\n\n## `new(T)` Function\n\n```go\n// Allocates memory, zero-initializes, returns pointer\np := new(int)\nfmt.Println(*p) // 0\n*p = 42\nfmt.Println(*p) // 42\n```\n\n## Zero Value: `nil`\n\n```go\nvar p *int\nfmt.Println(p == nil) // true\n// *p // PANIC: nil pointer dereference\nif p != nil { fmt.Println(*p) }\n```\n\n## No Pointer Arithmetic in Go\n\nUnlike C/C++, Go **does not support pointer arithmetic** — this is by design for safety. Use slice indexing instead.",
  "starterCode": "package main\n\nimport \"fmt\"\n\nfunc doubleValue(n int) {\n\tn *= 2 // only changes local copy\n}\n\nfunc doublePointer(n *int) {\n\t*n *= 2 // modifies the original\n}\n\nfunc swap(a, b *int) {\n\t*a, *b = *b, *a\n}\n\nfunc main() {\n\t// Basic pointer operations\n\tx := 42\n\tp := &x\n\n\tfmt.Printf(\"Value of x: %d\\n\", x)\n\tfmt.Printf(\"Address of x: %p\\n\", p)\n\tfmt.Printf(\"Value via pointer: %d\\n\", *p)\n\n\t// Modify via pointer\n\t*p = 100\n\tfmt.Printf(\"x after *p=100: %d\\n\", x)\n\n\t// new(T)\n\tpi := new(int)\n\tfmt.Printf(\"new(int) zero value: %d\\n\", *pi)\n\t*pi = 99\n\tfmt.Printf(\"new(int) after assign: %d\\n\", *pi)\n\n\t// nil pointer check\n\tvar nilPtr *int\n\tfmt.Println(\"nil pointer:\", nilPtr == nil)\n\n\t// Without pointer — no change\n\tval := 5\n\tdoubleValue(val)\n\tfmt.Println(\"After doubleValue:\", val)\n\n\t// With pointer — changes original\n\tdoublePointer(&val)\n\tfmt.Println(\"After doublePointer:\", val)\n\n\t// Swap using pointers\n\ta, b := 10, 20\n\tfmt.Printf(\"Before swap: a=%d, b=%d\\n\", a, b)\n\tswap(&a, &b)\n\tfmt.Printf(\"After swap: a=%d, b=%d\\n\", a, b)\n}",
  "quiz": [
    {
      "question_id": "Setelah `x := 42; p := &x; *p = 100`, nilai `x` menjadi berapa?",
      "question_en": "After `x := 42; p := &x; *p = 100`, what does `x` become?",
      "options_id": [
        "42 karena pointer hanya menyalin nilai",
        "100 karena dereference menulis ke variabel asli",
        "0 karena `x` direferensikan oleh pointer",
        "Tidak terdefinisi"
      ],
      "options_en": [
        "42 because the pointer only copied the value",
        "100 because dereferencing writes to the original variable",
        "0 because `x` is referenced by a pointer",
        "Undefined"
      ],
      "correct": 1
    },
    {
      "question_id": "Apa arti tipe `*int` di Go?",
      "question_en": "What does the type `*int` mean in Go?",
      "options_id": [
        "Sebuah integer yang harus diinisialisasi dengan `new`",
        "Sebuah pointer yang menunjuk ke nilai bertipe int",
        "Array dari integer",
        "Integer yang tidak bisa diubah"
      ],
      "options_en": [
        "An integer that must be initialized with `new`",
        "A pointer that points to a value of type int",
        "An array of integers",
        "An integer that cannot be changed"
      ],
      "correct": 1
    },
    {
      "question_id": "Apa zero value dari `var p *int`?",
      "question_en": "What is the zero value of `var p *int`?",
      "options_id": [
        "`0`",
        "`false`",
        "`nil`",
        "Pointer ke zero value int"
      ],
      "options_en": [
        "`0`",
        "`false`",
        "`nil`",
        "A pointer to the zero value of int"
      ],
      "correct": 2
    },
    {
      "question_id": "Mengapa `*p` pada nil pointer menyebabkan panic?",
      "question_en": "Why does `*p` on a nil pointer cause a panic?",
      "options_id": [
        "Karena compiler tidak tahu tipe dari `p`",
        "Karena tidak ada alamat valid yang bisa diakses",
        "Karena semua dereference dilarang di Go",
        "Karena pointer hanya boleh dibaca, bukan ditulis"
      ],
      "options_en": [
        "Because the compiler does not know the type of `p`",
        "Because there is no valid address to access",
        "Because all dereferencing is forbidden in Go",
        "Because pointers may only be read, not written"
      ],
      "correct": 1
    },
    {
      "question_id": "Apa yang dijamin oleh `new(int)`?",
      "question_en": "What does `new(int)` guarantee?",
      "options_id": [
        "Mengembalikan `int` bernilai 0",
        "Mengembalikan pointer ke `int` yang sudah dialokasikan dan diinisialisasi ke zero value",
        "Mengembalikan alamat dari variabel global",
        "Menghasilkan pointer yang tidak boleh diubah"
      ],
      "options_en": [
        "It returns an `int` with value 0",
        "It returns a pointer to an allocated `int` initialized to the zero value",
        "It returns the address of a global variable",
        "It produces a pointer that cannot be modified"
      ],
      "correct": 1
    },
    {
      "question_id": "Apa trade-off utama memakai pointer sebagai parameter fungsi dibanding value?",
      "question_en": "What is the main trade-off of using a pointer as a function parameter instead of a value?",
      "options_id": [
        "Pointer memungkinkan mutasi data asli, sedangkan value bekerja pada salinan",
        "Pointer selalu lebih sederhana dibaca",
        "Value tidak bisa dikirim ke fungsi",
        "Pointer menghilangkan kebutuhan nil check"
      ],
      "options_en": [
        "A pointer allows mutation of the original data, while a value works on a copy",
        "Pointers are always simpler to read",
        "Values cannot be passed to functions",
        "Pointers remove the need for nil checks"
      ],
      "correct": 0
    },
    {
      "question_id": "Mengapa fungsi `doubleValue(n int)` tidak mengubah variabel pemanggil?",
      "question_en": "Why doesn't `doubleValue(n int)` change the caller's variable?",
      "options_id": [
        "Karena integer immutable di Go",
        "Karena parameter fungsi menerima salinan nilai `n`",
        "Karena operator `*=` hanya berlaku pada pointer",
        "Karena Go selalu memakai pass-by-reference"
      ],
      "options_en": [
        "Because integers are immutable in Go",
        "Because the function parameter receives a copy of `n`",
        "Because the `*=` operator only works on pointers",
        "Because Go always uses pass-by-reference"
      ],
      "correct": 1
    },
    {
      "question_id": "Apa yang ditunjukkan fungsi `swap(a, b *int)`?",
      "question_en": "What does the function `swap(a, b *int)` demonstrate?",
      "options_id": [
        "Pointer arithmetic untuk mengubah alamat",
        "Dua pointer bisa dipakai untuk memodifikasi dua variabel pemanggil secara langsung",
        "Pointer hanya berguna untuk membaca nilai",
        "Swap di Go hanya bisa dilakukan dengan return tuple"
      ],
      "options_en": [
        "Pointer arithmetic to change addresses",
        "Two pointers can be used to modify two caller variables directly",
        "Pointers are only useful for reading values",
        "Swapping in Go can only be done by returning a tuple"
      ],
      "correct": 1
    },
    {
      "question_id": "Apa alasan Go tidak mendukung pointer arithmetic menurut pelajaran ini?",
      "question_en": "According to this lesson, why does Go not support pointer arithmetic?",
      "options_id": [
        "Karena compiler Go tidak mampu mengimplementasikannya",
        "Untuk keamanan; gunakan indexing slice sebagai gantinya",
        "Karena pointer hanya ada untuk struct",
        "Agar semua program menjadi lebih cepat"
      ],
      "options_en": [
        "Because the Go compiler is not capable of implementing it",
        "For safety; use slice indexing instead",
        "Because pointers exist only for structs",
        "So every program becomes faster"
      ],
      "correct": 1
    },
    {
      "question_id": "Pilihan mana yang paling idiomatis untuk memakai pointer dengan aman?",
      "question_en": "Which choice is the most idiomatic way to use pointers safely?",
      "options_id": [
        "Selalu dereference tanpa cek karena zero value aman",
        "Hindari semua pointer di Go",
        "Cek `p != nil` sebelum dereference jika pointer mungkin belum terinisialisasi",
        "Konversi semua pointer menjadi `uintptr`"
      ],
      "options_en": [
        "Always dereference without checking because zero values are safe",
        "Avoid all pointers in Go",
        "Check `p != nil` before dereferencing if the pointer may be uninitialized",
        "Convert every pointer to `uintptr`"
      ],
      "correct": 2
    }
  ]
}
